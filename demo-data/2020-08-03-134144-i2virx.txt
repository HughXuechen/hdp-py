yes comment elf years years comment always bad math despite language get really numbers comment years old comment way comment elf yes comment even expect way text rather things character character like expect sees encoded text character encoded characters even makes hard character things way character encoded characters math digit digit actually sees digit digit numbers makes really hard arithmetic gpt arithmetic despite actually comment fundamental design like bad point models get improvements fundamental design rather bigger model things comment make model bigger get improvements size model gpt times big gpt gpt model times big size model even make better eventually point language models eventually comment better comment comment like big dwarf comment dwarf actually joseph comment always character old like comment comment joseph comment